# Configuration values for application.  Don't commit private copy!
DEBUG = True
SECRET_KEY = 'generate some random secret string'
CONSUMER_KEY = '<value from FB>'
CONSUMER_SECRET = '<value from FB>'
SQLALCHEMY_DATABASE_URI = 'postgresql://user:password@localhost/dbname'
PORTAL = '<base URI for this installation>'
DECIDING_INTERVENTION = '<entry URI for intervention>'
SELF_MANAGE_INTERVENTION = '<entry URI for intervention>'
