# Configuration values for application.  Don't commit private copy!
DEBUG = True
SECRET_KEY = 'generate some random secret string'
FB_CONSUMER_KEY = '<App ID from Facebook>'
FB_CONSUMER_SECRET = '<App Secret from Facebook>'
SQLALCHEMY_DATABASE_URI = 'postgresql://user:password@localhost/dbname'
SERVER_NAME = '<the name and port number of the server (e.g.: 'myapp.dev:5000')>'
